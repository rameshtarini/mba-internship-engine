# Contributing

To add a new company, edit `data/companies.yaml` and add a new YAML block with the following fields:

- `name`
- `tier`
- `boards`
- `careers_url`
- `notes`
- `typical_open` (optional)

Each board entry should include `platform` and `slug`.
